# UI Design Specification - 갓생(God Life)

## 1. 컬러 시스템 (Color System)

### 1.1 브랜드 컬러

| 컬러명         | HEX     | RGB          | 용도                         |
| -------------- | ------- | ------------ | ---------------------------- |
| Primary Indigo | #4F46E5 | 79, 70, 229  | 주요 CTA, 활성 상태          |
| Primary Purple | #7C3AED | 124, 58, 237 | 보조 강조, Faith Mode 액센트 |
| Primary Amber  | #F59E0B | 245, 158, 11 | 성취, 연속 달성, 알림        |

### 1.2 테마별 컬러 팔레트

#### Faith Mode (신앙 모드)

| 토큰명                 | Light Mode | Dark Mode | 용도           |
| ---------------------- | ---------- | --------- | -------------- |
| `faith-bg-primary`     | #FEFEFE    | #0F0D15   | 메인 배경      |
| `faith-bg-secondary`   | #F5F3FF    | #1E1B2E   | 카드/섹션 배경 |
| `faith-bg-tertiary`    | #EDE9FE    | #2D2640   | 입력필드 배경  |
| `faith-text-primary`   | #1F1A38    | #F5F3FF   | 본문 텍스트    |
| `faith-text-secondary` | #6B5B95    | #A89EC9   | 보조 텍스트    |
| `faith-text-muted`     | #9D92B8    | #6B5B95   | 비활성 텍스트  |
| `faith-accent`         | #7C3AED    | #A78BFA   | 강조 요소      |
| `faith-accent-soft`    | #DDD6FE    | #3D2E66   | 강조 배경      |
| `faith-border`         | #E9E3F5    | #3D2E66   | 테두리         |
| `faith-success`        | #10B981    | #34D399   | 성공/완료      |
| `faith-warning`        | #F59E0B    | #FBBF24   | 주의/경고      |
| `faith-error`          | #EF4444    | #F87171   | 에러           |

#### Universal Mode (범용 모드)

| 토큰명               | Light Mode | Dark Mode | 용도           |
| -------------------- | ---------- | --------- | -------------- |
| `uni-bg-primary`     | #FFFFFF    | #0A0A0B   | 메인 배경      |
| `uni-bg-secondary`   | #F4F4F5    | #18181B   | 카드/섹션 배경 |
| `uni-bg-tertiary`    | #E4E4E7    | #27272A   | 입력필드 배경  |
| `uni-text-primary`   | #09090B    | #FAFAFA   | 본문 텍스트    |
| `uni-text-secondary` | #52525B    | #A1A1AA   | 보조 텍스트    |
| `uni-text-muted`     | #A1A1AA    | #71717A   | 비활성 텍스트  |
| `uni-accent`         | #4F46E5    | #818CF8   | 강조 요소      |
| `uni-accent-soft`    | #E0E7FF    | #312E81   | 강조 배경      |
| `uni-border`         | #E4E4E7    | #3F3F46   | 테두리         |
| `uni-success`        | #22C55E    | #4ADE80   | 성공/완료      |
| `uni-warning`        | #EAB308    | #FACC15   | 주의/경고      |
| `uni-error`          | #EF4444    | #F87171   | 에러           |

### 1.3 시맨틱 컬러

```dart
// Flutter 구현 예시
class AppColors {
  // 루틴 달성도
  static const Color streakGold = Color(0xFFF59E0B);
  static const Color streakSilver = Color(0xFF94A3B8);
  static const Color streakBronze = Color(0xFFCD7F32);

  // 진행률
  static const Color progressComplete = Color(0xFF10B981);
  static const Color progressInProgress = Color(0xFF3B82F6);
  static const Color progressNotStarted = Color(0xFFE5E7EB);

  // AI 코칭 스타일
  static const Color styleFeeling = Color(0xFFEC4899);    // F 스타일
  static const Color styleThinking = Color(0xFF06B6D4);   // T 스타일
}
```

---

## 2. 타이포그래피 (Typography)

### 2.1 폰트 패밀리

| 용도      | 한글       | 영문       | Fallback                          |
| --------- | ---------- | ---------- | --------------------------------- |
| 제목      | Pretendard | Pretendard | -apple-system, BlinkMacSystemFont |
| 본문      | Pretendard | Pretendard | Helvetica Neue, sans-serif        |
| 숫자/코드 | Pretendard | SF Mono    | monospace                         |

### 2.2 타이포그래피 스케일

| 토큰명       | 크기 | 행간 | 자간    | 굵기           | 용도             |
| ------------ | ---- | ---- | ------- | -------------- | ---------------- |
| `display-lg` | 36px | 44px | -0.02em | Bold (700)     | 대형 타이틀      |
| `display-md` | 30px | 38px | -0.02em | Bold (700)     | 섹션 타이틀      |
| `display-sm` | 24px | 32px | -0.01em | SemiBold (600) | 카드 타이틀      |
| `heading-lg` | 20px | 28px | -0.01em | SemiBold (600) | 서브 타이틀      |
| `heading-md` | 18px | 26px | 0       | SemiBold (600) | 리스트 헤더      |
| `heading-sm` | 16px | 24px | 0       | Medium (500)   | 소제목           |
| `body-lg`    | 16px | 26px | 0       | Regular (400)  | 본문 (강조)      |
| `body-md`    | 14px | 22px | 0       | Regular (400)  | 기본 본문        |
| `body-sm`    | 13px | 20px | 0       | Regular (400)  | 보조 텍스트      |
| `caption`    | 12px | 16px | 0.01em  | Regular (400)  | 캡션, 타임스탬프 |
| `overline`   | 11px | 16px | 0.08em  | Medium (500)   | 레이블, 태그     |

### 2.3 Flutter TextStyle 구현

```dart
class AppTypography {
  static const TextStyle displayLg = TextStyle(
    fontSize: 36,
    height: 1.22,  // 44/36
    letterSpacing: -0.72,
    fontWeight: FontWeight.w700,
    fontFamily: 'Pretendard',
  );

  static const TextStyle bodyMd = TextStyle(
    fontSize: 14,
    height: 1.57,  // 22/14
    letterSpacing: 0,
    fontWeight: FontWeight.w400,
    fontFamily: 'Pretendard',
  );

  // ... 나머지 스타일
}
```

---

## 3. 간격 시스템 (Spacing System)

### 3.1 기본 그리드

**Base Unit: 4px**

| 토큰       | 값   | 용도               |
| ---------- | ---- | ------------------ |
| `space-0`  | 0px  | -                  |
| `space-1`  | 4px  | 아이콘-텍스트 간격 |
| `space-2`  | 8px  | 관련 요소 간 간격  |
| `space-3`  | 12px | 컴포넌트 내부 패딩 |
| `space-4`  | 16px | 기본 여백          |
| `space-5`  | 20px | 카드 패딩          |
| `space-6`  | 24px | 섹션 간 간격       |
| `space-8`  | 32px | 큰 섹션 간격       |
| `space-10` | 40px | 페이지 상단 여백   |
| `space-12` | 48px | 주요 영역 구분     |
| `space-16` | 64px | 화면 전환 영역     |

### 3.2 컴포넌트별 간격 규칙

```
┌─────────────────────────────────────────┐
│  Screen Padding: 16px (좌우)            │
│  ┌─────────────────────────────────┐    │
│  │  Card Padding: 20px             │    │
│  │  ┌───────────────────────────┐  │    │
│  │  │  Title (heading-md)       │  │    │
│  │  │  ↓ 8px                    │  │    │
│  │  │  Subtitle (body-sm)       │  │    │
│  │  │  ↓ 16px                   │  │    │
│  │  │  Content Area             │  │    │
│  │  └───────────────────────────┘  │    │
│  └─────────────────────────────────┘    │
│  ↓ 12px (카드 간 간격)                  │
│  ┌─────────────────────────────────┐    │
│  │  Next Card...                   │    │
└─────────────────────────────────────────┘
```

### 3.3 SafeArea 및 시스템 여백

```dart
class AppSpacing {
  // 시스템
  static const double safeAreaTop = 44.0;      // iOS 노치
  static const double safeAreaBottom = 34.0;  // iOS 홈 인디케이터
  static const double statusBarHeight = 20.0;  // Android

  // GNB
  static const double gnbHeight = 56.0;
  static const double gnbPaddingBottom = 16.0;  // safe area 제외

  // 화면
  static const double screenPadding = 16.0;
  static const double cardPadding = 20.0;
  static const double sectionGap = 24.0;
}
```

---

## 4. 컴포넌트 스펙 (Component Specifications)

### 4.1 버튼 (Buttons)

#### Primary Button

```
┌──────────────────────────────────────┐
│            Button Text               │
└──────────────────────────────────────┘

- 높이: 52px
- Border Radius: 12px
- 패딩: 16px 24px
- 폰트: body-lg, SemiBold (600)
- 배경: accent
- 텍스트: white
- 비활성: opacity 0.5
- 눌림: scale 0.98, brightness -10%
```

#### Secondary Button

```
- 높이: 52px
- Border Radius: 12px
- 패딩: 16px 24px
- 폰트: body-lg, SemiBold (600)
- 배경: transparent
- 테두리: 1.5px solid accent
- 텍스트: accent
```

#### Small Button

```
- 높이: 36px
- Border Radius: 8px
- 패딩: 8px 16px
- 폰트: body-sm, Medium (500)
```

#### Icon Button

```
- 크기: 44px × 44px (터치 영역)
- 아이콘 크기: 24px
- Border Radius: 10px
```

### 4.2 입력 필드 (Input Fields)

#### Text Field

```
┌─────────────────────────────────────────┐
│ Label (caption, text-secondary)         │
│ ↓ 6px                                   │
│ ┌─────────────────────────────────────┐ │
│ │ 🔍  Placeholder text...             │ │
│ └─────────────────────────────────────┘ │
│ ↓ 4px                                   │
│ Helper text (caption, text-muted)       │
└─────────────────────────────────────────┘

- 높이: 52px
- Border Radius: 12px
- 패딩: 16px
- 아이콘 왼쪽 패딩: 44px (16 + 24 + 4)
- 배경: bg-tertiary
- 테두리: 1px solid border (기본)
- 테두리 포커스: 2px solid accent
- 에러: 테두리 error, Helper text error색
```

### 4.3 카드 (Cards)

#### 기본 카드

```
┌─────────────────────────────────────────┐
│                                         │
│  Content Area                           │
│                                         │
└─────────────────────────────────────────┘

- Border Radius: 16px
- 패딩: 20px
- 배경: bg-secondary
- 그림자: 0px 2px 8px rgba(0,0,0,0.04)
- Dark Mode 그림자: 0px 2px 8px rgba(0,0,0,0.2)
```

#### 인사이트 카드 (오늘의 말씀/명언)

```
┌─────────────────────────────────────────┐
│  📖                                     │
│                                         │
│  "말씀/명언 내용이 여기에               │
│   들어갑니다."                          │
│                                         │
│  출처 - 성경구절/저자       공유 버튼 → │
└─────────────────────────────────────────┘

- Border Radius: 20px
- 패딩: 24px
- 배경: 그라디언트 (faith-accent-soft to faith-bg-secondary)
- 최소 높이: 160px
```

#### 루틴 카드

```
┌─────────────────────────────────────────┐
│  ☐  루틴 이름                    🔥 7   │
│      06:30 AM · 매일                    │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
└─────────────────────────────────────────┘

- Border Radius: 14px
- 패딩: 16px
- 높이: 약 80px
- 체크박스 크기: 24px
- 진행률 바 높이: 4px
- 연속 달성 배지: 뱃지 스타일 (배경: amber-soft)
```

### 4.4 바텀 네비게이션 (GNB)

```
┌─────────────────────────────────────────┐
│   🏠      📋      💬      ⚙️          │
│   홈      루틴    코칭    설정          │
└─────────────────────────────────────────┘

- 전체 높이: 56px + safe area
- 아이콘 크기: 24px
- 텍스트: caption (12px)
- 아이콘-텍스트 간격: 4px
- 활성 상태: accent 색상, 아이콘 filled
- 비활성 상태: text-muted, 아이콘 outlined
- 배경: bg-primary + blur backdrop
- 상단 테두리: 0.5px solid border
```

### 4.5 채팅 버블 (AI Coaching)

#### 사용자 메시지

```
                    ┌─────────────────────┐
                    │  사용자 메시지 내용  │
                    └─────────────────────┘
                                    14:32

- 배경: accent
- 텍스트: white
- Border Radius: 16px 16px 4px 16px
- 패딩: 12px 16px
- 최대 너비: 75%
```

#### AI 메시지

```
  🤖
  ┌─────────────────────────────────────┐
  │  AI 응답 메시지 내용                 │
  │  여러 줄에 걸쳐 표시될 수 있습니다.  │
  └─────────────────────────────────────┘
  14:33

- 배경: bg-secondary
- 텍스트: text-primary
- Border Radius: 16px 16px 16px 4px
- 패딩: 12px 16px
- 최대 너비: 85%
- 아바타 크기: 32px
```

---

## 5. 아이콘 (Icons)

### 5.1 아이콘 스펙

| 용도          | 크기 | Stroke Width |
| ------------- | ---- | ------------ |
| GNB           | 24px | 1.5px        |
| 카드 내       | 20px | 1.5px        |
| 버튼 내       | 20px | 2px          |
| 인라인        | 16px | 1.5px        |
| 대형 일러스트 | 48px | 1px          |

### 5.2 주요 아이콘 목록

```
Navigation:
- home (홈)
- list-checks (루틴)
- message-circle (코칭)
- settings (설정)

Routine:
- check-circle (완료)
- circle (미완료)
- flame (연속 달성)
- calendar (캘린더)
- clock (시간)
- repeat (반복)
- bell (알림)

Coaching:
- sparkles (AI)
- heart (공감 - F스타일)
- brain (분석 - T스타일)
- file-text (리포트)

Social:
- users (그룹)
- trophy (챌린지)
- share (공유)
- thumbs-up (응원)

System:
- chevron-right (이동)
- plus (추가)
- x (닫기)
- more-horizontal (더보기)
```

---

## 6. 애니메이션 (Animations)

### 6.1 타이밍 함수

| 토큰      | Duration | Easing      | 용도           |
| --------- | -------- | ----------- | -------------- |
| `instant` | 100ms    | ease-out    | 버튼 눌림      |
| `fast`    | 200ms    | ease-in-out | 토글, 체크박스 |
| `normal`  | 300ms    | ease-in-out | 화면 전환      |
| `slow`    | 500ms    | ease-in-out | 모달 등장      |

### 6.2 주요 애니메이션

#### 버튼 피드백

```dart
onTapDown: scale(0.98), duration: 100ms
onTapUp: scale(1.0), duration: 200ms
```

#### 루틴 체크

```dart
// 체크 시
scale: 1.0 → 1.2 → 1.0 (300ms)
opacity: 0 → 1 (200ms)
confetti: trigger (선택적)
```

#### 카드 등장

```dart
// 리스트 카드
offset: (0, 20) → (0, 0)
opacity: 0 → 1
duration: 300ms
stagger: 50ms (카드 간 딜레이)
```

#### 화면 전환

```dart
// 같은 레벨 이동 (GNB)
type: fade
duration: 200ms

// 상세 화면 이동
type: slideFromRight
duration: 300ms

// 모달
type: slideFromBottom
duration: 400ms
```

---

## 7. 반응형 기준 (Responsive Breakpoints)

| 디바이스 | 너비      | 적용               |
| -------- | --------- | ------------------ |
| Mobile S | < 375px   | 최소 지원          |
| Mobile M | 375-414px | 기본 디자인        |
| Mobile L | 414-480px | 여유 있는 레이아웃 |
| Tablet   | > 480px   | 2컬럼 가능         |

### 적응형 레이아웃

```dart
// 인사이트 카드
if (width < 375) {
  padding: 16px;
  fontSize: body-sm;
} else {
  padding: 24px;
  fontSize: body-lg;
}

// 루틴 리스트
if (width > 480) {
  gridColumns: 2;
} else {
  gridColumns: 1;
}
```

---

## 8. 접근성 (Accessibility)

### 8.1 컬러 대비

모든 텍스트-배경 조합은 WCAG AA 기준 충족:

- 일반 텍스트: 최소 4.5:1
- 대형 텍스트 (18px+): 최소 3:1

### 8.2 터치 타겟

- 최소 크기: 44px × 44px
- 요소 간 간격: 최소 8px

### 8.3 스크린 리더

모든 인터랙티브 요소에 semantic label 제공:

```dart
Semantics(
  label: '루틴 완료하기: 아침 기도',
  button: true,
  child: CheckboxWidget(),
)
```
